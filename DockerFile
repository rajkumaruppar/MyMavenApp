# Step 1: Use the official Tomcat image from Docker Hub
FROM tomcat:9-jdk11-openjdk

# Step 2: Set the working directory
WORKDIR /usr/local/tomcat/webapps

# Step 3: Copy the WAR file into the Tomcat webapps folder
# Replace 'target/myapp.war' with the actual path to your WAR file
COPY target/*.war /usr/local/tomcat/webapps/ROOT.war

# Step 4: Expose port 8000 (default Tomcat port is 8080, change it to 8000)
EXPOSE 8000

