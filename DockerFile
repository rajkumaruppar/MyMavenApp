# Use the official Tomcat 9 image as the base image
FROM tomcat:9-jdk11-openjdk

# Set the working directory
WORKDIR /usr/local/tomcat/webapps

# Copy the WAR file from the target directory to the Tomcat webapps directory
# Adjust the path if your WAR file name differs from 'MyMavenApp.war'
COPY target/MyMavenApp.war /usr/local/tomcat/webapps/ROOT.war

# Expose port 8000 (or any other port you prefer)
EXPOSE 8000

# Start Tomcat
CMD ["catalina.sh", "run"]


